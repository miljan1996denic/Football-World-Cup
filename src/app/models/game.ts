export class Game {
    constructor(
        public id: number,
        public home: string,
        public guest: string,
        public goalHome:number,
        public goalGuest:number,
    ) {

    }
}